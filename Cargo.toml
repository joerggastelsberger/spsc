[package]
name = "spsc"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
core_affinity = "0.8.3"
criterion = { version = "0.8.2", features = ["html_reports"] }

[target.'cfg(loom)'.dependencies]
loom = "0.7"

[[bench]]
name = "padded_vs_unpadded"
harness = false
